# Rust build artifacts
target/

# TLS certificates (regenerated during build)
certs/

# OS
.DS_Store

# IDEs
.vscode/
.idea/
.VSCodeCounter/

# Tools
cspell.config.yaml

# Git
.git/
.gitmodules

# Docker (avoid recursive context)
docker-compose.yml

# UI build artifacts (rebuilt in container)
m0n1t0r-ui/node_modules/
m0n1t0r-ui/dist/
